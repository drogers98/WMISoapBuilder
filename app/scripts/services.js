'use strict';
angular.module('WMISoapBuilder.services', [])

/**
 * A simple example service that returns some data.
 */

.factory('Responders', function() {
  
})

.factory('Soaps', function() {

});
